-- Optional: Profile für alle authentifizierten Nutzer lesbar (nur SELECT)
drop policy if exists "profiles_select_own" on public.profiles;
create policy "profiles_select_all"
on public.profiles for select
to authenticated
using (true);
